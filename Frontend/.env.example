# Frontend Environment Variables Example
# In development (with Vite proxy) or Docker Nginx, keep empty or unset.
# In production (when deployed separately on Vercel with backend on Render), set your backend URL.
# Example: VITE_API_BASE_URL=https://recordvault-backend.onrender.com

VITE_API_BASE_URL=
